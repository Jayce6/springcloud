spring:
  cloud:
    config:
      name: microservicecloud-config-client #需要从github上读取资源的名称
      profile: dev
      label: master #本次访问的配置项
      uri: http://config-3344.com:3344 #本微服务启动后先去找3344号服务。通过SpringCloudConfig的GitHub地址
